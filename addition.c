#include <stdio.h>
int main() {    

    int change1, number2, sum;
    
    change1=1;number2=2;    
    // calculating sum
    sum = change1 + number2;      
    
    printf("%d + %d = %d", change1, number2, sum);
    return 0;
}
